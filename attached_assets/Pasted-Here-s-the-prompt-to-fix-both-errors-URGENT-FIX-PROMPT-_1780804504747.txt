Here's the prompt to fix both errors:

---

**URGENT FIX PROMPT — SAMMY STORE (evilos619-cell/sammystore)**

Two critical issues need to be fixed immediately:

---

**FIX 1 — GitHub CI/CD Build Failure ("All jobs have failed")**

The GitHub Actions CI workflow is failing at the build step (failed in 1 minute 22 seconds, 2 annotations).

- Go to `.github/workflows/` and open the CI workflow file
- Read the full error logs from the failed build — check for: missing dependencies, wrong Node/Python version specified, missing environment variables in the workflow, build command errors, or missing secrets
- Fix the root cause of the CI failure
- If the workflow is referencing secrets like `PAYSTACK_SECRET_KEY`, `NOWPAYMENTS_API_KEY`, `DATABASE_URL`, `CLOUDINARY_URL` etc., make sure they are added to the GitHub repository under **Settings → Secrets and Variables → Actions**
- If the workflow is unnecessary or misconfigured, either fix it properly or remove it and replace with a working one
- A basic working CI workflow should: install dependencies → run build → confirm success
- After fixing, commit and push so the CI runs again and passes

---

**FIX 2 — Image Upload Still Failing ("Upload failed: Bucket not found")**

The image upload error is still occurring even after the previous fix attempt. This means the storage backend was never properly configured.

- Check the current image upload handler — identify exactly which storage it's trying to use (Supabase bucket, AWS S3, or other)
- If it's Supabase storage: either create the bucket in the Supabase dashboard and set the correct bucket name in `.env`, or switch away from Supabase storage entirely
- **Recommended fix:** Replace the broken storage with **Cloudinary** (free tier):
  - Install: `npm install cloudinary multer multer-storage-cloudinary`
  - Add to `.env`:
    ```
    CLOUDINARY_CLOUD_NAME=your_cloud_name
    CLOUDINARY_API_KEY=your_api_key
    CLOUDINARY_API_SECRET=your_api_secret
    ```
  - Update the upload route to use Cloudinary's multer storage
  - Return the Cloudinary secure URL and save it to the product's image field in the database
  - Show image preview in the form after successful upload
- Test the upload with a real image before committing
- Make sure the `.env` variables are also added to Replit Secrets so the live site works

---

**AFTER BOTH FIXES:**
- Stage and commit all changes:
```
git add .
git commit -m "fix: resolve CI build failure and replace broken image upload with Cloudinary"
git push origin main
```
- Confirm CI workflow passes on GitHub after the push
- Confirm image upload works on the live site at mmystorelogs.com

---